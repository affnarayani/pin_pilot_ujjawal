name: 2A. Update Workflow Timings

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: {}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: ci-${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-timings:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Run timing.py
        run: python update_timing.py

      - name: Commit and push changes
        shell: bash
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN_ALL }}
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore: update workflow cron times from timing.py [skip ci]"

          # Authentication ke liye PAT ka use
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/${{ github.repository }}.git

          # Force rebase and push
          git pull origin main --rebase --no-edit
          git push origin main